library 'test_lib'

pipeline {
    agent any

    // environment {
    //     CREDENTIALS1 = credentials('GOOGLE_CRED')
    //     CREDENTIALS2 = credentials('CRED2')
    // }
    stages {
        stage('Hello') {
            steps {
//                withCredentials([file(credentialsId: 'GOOGLE_KEY', variable: 'FILE')]) {
//                runScript()
//                }
            }
        }
    }
}
